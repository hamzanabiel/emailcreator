# Core dependencies
pandas>=2.0.0
pyyaml>=6.0
jinja2>=3.1.0

# Email handling (cross-platform)
# For Windows: pywin32 will be used for .msg creation
# For Linux/Mac: python-email and eml format
pywin32>=305; platform_system == "Windows"

# Cross-platform .msg handling - CORRECTED PACKAGE NAME
extract-msg>=0.49.0

# Alternative lightweight option (uncomment if you prefer this over extract-msg)
# msglite>=0.30.0

# Another modern alternative (uncomment if you prefer this)
# python-oxmsg>=0.0.2

# Utilities
colorama>=0.4.6
tqdm>=4.65.0
